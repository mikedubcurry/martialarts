import Authenticated from "@/Layouts/AuthenticatedLayout";

export default function Create({ user }) {
    return (
        <Authenticated>
            <h1>Create</h1>
        </Authenticated>
    );
}
